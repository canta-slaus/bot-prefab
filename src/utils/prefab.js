const PREFIX = require('../../config/config.json').PREFIX;

module.exports = {
    name: "",
    aliases: [],
    description: "",
    usage: `\`${PREFIX}\``,
    examples: `\`${PREFIX}\``,
    cooldown: 0,
    perms: [],
    
    execute: async function(client, message, args) {

    }
}